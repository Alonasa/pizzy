<!--Render register page form-->
<%- include("chunks/form-wrapper") %>
<% scripts = `
<script type="module">
   import showPopup from "./js/popups.js";
   import showHidePassword from "./js/showPassword.js";

   showHidePassword();
    //Show the modal if there's an error in the URL
    const urlParams = new URLSearchParams(window.location.search);
    if (urlParams.has("error")) {
        showPopup("Wrong email or password. Please check them and try again", "error", "center");
    }
</script>
`; %>